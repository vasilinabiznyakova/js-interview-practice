/* Live Coding #2 — groupBy

Напиши функцию groupBy(arr, keyFn).

Вход:

const users = [
  { id: 1, city: 'Lisbon' },
  { id: 2, city: 'Porto' },
  { id: 3, city: 'Lisbon' },
];


Ожидаемый результат:

{
  Lisbon: [{ id: 1, city: 'Lisbon' }, { id: 3, city: 'Lisbon' }],
  Porto: [{ id: 2, city: 'Porto' }],
}


Требования:

O(n)

без библиотек

можно JS или TS

Напиши решение. */

// O(n);

const users = [
  { id: 1, city: 'Lisbon' },
  { id: 2, city: 'Porto' },
  { id: 3, city: 'Lisbon' },
];

const key = 'city';

function groupBy(arr) {
    
}

groupBy(arr);
